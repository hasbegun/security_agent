# Use an official Python runtime as a parent image
FROM python:3.13-slim

# Set environment variables to prevent Python from buffering stdout/stderr
ENV PYTHONUNBUFFERED=1
# Define the application directory within the container
ENV APP_HOME=/app

# Create and set the working directory
WORKDIR $APP_HOME

# Install system dependencies that might be needed by some Python packages
# (e.g., build tools, libraries for specific embeddings or document loaders)
# Add any specific system dependencies your tools might require here if needed.
# RUN apt-get update && apt-get install -y --no-install-recommends gcc build-essential && rm -rf /var/lib/apt/lists/*

# Copy the requirements file into the container
COPY requirements.txt .

# Install Python dependencies
RUN pip install --no-cache-dir --upgrade pip
RUN pip install --no-cache-dir -r requirements.txt

# Copy the application code into the container
# Ensure data directories are copied if not mounted via volume later
COPY ./app ./app
COPY ./data ./data

# Expose the port the app runs on
EXPOSE 8000

# Specify the command to run on container start
# Run using uvicorn, accessible from outside the container on 0.0.0.0
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]